<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
    https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <entity name="StockFlow\Catalog\Domain\Entity\Product" table="catalog_products">
        <id name="id" type="integer" column="id">
            <generator strategy="IDENTITY"/>
        </id>

        <field name="name" column="name"/>

        <embedded name="sku" class="StockFlow\Shared\Catalog\Domain\ValueObject\Sku"/>

        <one-to-many field="attributes"
                     target-entity="StockFlow\Catalog\Domain\Entity\ProductAttribute"
                     mapped-by="product"
                     orphan-removal="true">
            <cascade>
                <cascade-all/>
            </cascade>
        </one-to-many>

        <one-to-many field="images"
                     target-entity="StockFlow\Catalog\Domain\Entity\ProductImage"
                     mapped-by="product"
                     orphan-removal="true">
            <cascade>
                <cascade-all/>
            </cascade>
        </one-to-many>

        <field name="createdAt" type="datetime_immutable" column="created_at"/>
        <field name="updatedAt" type="datetime_immutable" column="updated_at" nullable="true"/>

        <lifecycle-callbacks>
            <lifecycle-callback type="prePersist" method="setInitialTimestamps"/>
            <lifecycle-callback type="preUpdate" method="setUpdateTimestamp"/>
        </lifecycle-callbacks>
    </entity>

</doctrine-mapping>
